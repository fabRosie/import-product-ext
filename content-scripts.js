document.body.style.backgroundColor = 'blue';
const box = document.querySelector('.product-action');
let btn = document.createElement('button')
let btnText=document.createTextNode('import!!')
btn.appendChild(btnText)
box.appendChild(btn)